home = document.querySelector("#home")

home.style.fontWeight = "bold";
home.style.textDecoration = 'underline'; // Ensures the underline is applied
home.style.textUnderlineOffset = '13px'; // Adds offset to the underline

